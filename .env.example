# ============================================================
# CryptoEats — Environment Variables
# ============================================================
# Copy this file to .env and fill in the values.
# Variables marked [REQUIRED] must be set for the app to run.
# Variables marked [OPTIONAL] have sensible defaults.
# ============================================================

# ── Database ─────────────────────────────────────────────────
# [REQUIRED] PostgreSQL connection string
# On Replit this is auto-provisioned. For local dev use your own Postgres instance.
DATABASE_URL=postgresql://user:password@localhost:5432/cryptoeats

# Individual Postgres connection params (used by some tooling)
PGHOST=localhost
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=cryptoeats

# ── Authentication ───────────────────────────────────────────
# [REQUIRED] Secret used to sign JWT tokens. Use a long random string.
SESSION_SECRET=your-secure-random-secret-here

# ── Server ───────────────────────────────────────────────────
# [OPTIONAL] Port the Express server listens on (default: 5000)
PORT=5000

# ── Frontend / Expo ──────────────────────────────────────────
# [REQUIRED for mobile] Domain the Expo client uses to reach the API.
# On Replit this is set automatically via REPLIT_DEV_DOMAIN.
EXPO_PUBLIC_DOMAIN=https://your-repl.replit.dev

# ── Replit-Specific (auto-set by Replit) ─────────────────────
# These are injected automatically when running on Replit.
# You do NOT need to set them manually.
# REPLIT_DEV_DOMAIN=
# REPLIT_DOMAINS=

# ── Open Platform / Developer API ────────────────────────────
# [OPTIONAL] Enable the interactive API playground in Swagger docs (default: false)
API_PLAYGROUND_ENABLED=true

# [OPTIONAL] Secret used to sign outbound webhook payloads (HMAC-SHA256)
WEBHOOK_SECRET=your-webhook-signing-secret

# ── Blockchain / Base Chain ──────────────────────────────────
# [OPTIONAL] Base network to use: "mainnet" (chain 8453) or "sepolia" (chain 84532)
# Defaults to sepolia for development.
BASE_NETWORK=sepolia
